# 认知OS：顶层设计

## 前言：Transformer的问题

在之前与AI的Brainstorming中，我提出了一个有趣的想法：Transformer在抽象不同的模式、从大量无规律数据（Token序列）中提取隐式规律，在不同的符号体系间迁移规律这几件事上干的非常棒，这也是为什么现在以LLM为首的Transformer系AI如此强大和火热的根本原因。

但Transformer自己无法做到以下几件它内禀就没法很好完成的事情：

- 严密的逻辑推理（因为它本质上是基于统计概率的，不能做到100%的严丝合缝；CoT虽然改善了一部分前向传播的回溯问题，但整个体系依然是难以回溯的；且自然语言由于歧义性的存在，也不是逻辑推理的最佳载体）
- 持续学习，也就是从交互和学习中内化记忆，并固化成常识和经验的能力，毕竟梯度下降和“训练-推理”的二元对立就很不适合干这个
- 对我们生存的这个世界没有物理概念和直觉（也就是“世界模型”）
- “万物皆Token，皆可Tokenize”催生了Transformer生态的繁荣，但计科的历史告诉我们，通用表示作为一种抽象，必然会导致特定结构的隐性化。显然，Transformer需要花费额外的工夫（Attention机制的算力），透过对大量标记数据的学习，在其网络内部透过Attention机制，重新把隐藏在无结构数据中的隐式结构显性化。这就带来了额外的算力消耗。
- Transformer的Attention机制，以及“Token需要两两交互”的原理决定这套系统是反稀疏的，必然导致整套系统在算力消耗上会持续膨胀。任何MoE和其他“模拟稀疏计算”的工程手段必然是治标不治本。

而对这些问题，我提出了一个有趣的想法：认知系统（Cognitive OS）。接下来，我将对这个概念进行详细阐述。

## 异构思想与AI

“认知OS”的设计理念来源于计算机体系架构的“异构思想“。以下我会对这个计算机体系架构的设计思想进行阐述，并将其类比到AI的架构设计中去。

### 什么是异构思想

#### CPU的诞生、发展和瓶颈

在计算机体系架构中，CPU是整个系统的核心组件。它是冯·诺伊曼架构“算术逻辑单元（CA）“和”中央控制单元（CC）”的综合，是整个计算机体系的核心，负责处理所有的运算和控制任务。在当时的架构下，CPU需要独揽一切：最开始浮点运算要用整数运算+科学计算来模拟；所有的像素点全部映射在内存中的FrameBuffer区域，CPU必须自己硬算这些像素点；I/O一开始靠轮询，后来靠中断，都算不上是多么高效的方式，有了DMA才解决这个问题。由于早期总线技术不成熟和晶体管单价太贵，所以我们只能做一个高度同构、承担一切的全能CPU；而自然，这个CPU的性能上限，自然也成了整个系统最大的性能瓶颈。

所以，在早期阶段，为了提升整个系统执行任务的性能，最常用、在工程上也最有效的方法，就是提升CPU本身执行指令的效率。在异构运算诞生之前，工程师们在这条路上做了很久，也作出了很多很有用的成果出来。

早期的计算机面临一个问题——早期的RAM（磁氧体和早期DRAM）又慢又贵，大小和读取速度都令人捉急。为了解决这两个问题，工程师们发明了复杂指令集（CISC）和微码（Microcode），用单条指令表示一个复杂操作，并在CPU内部用微码展开成若干个微指令去执行。这算是一种提升系统效率的方法。但这么干并非没有代价——复杂指令集的译码电路很复杂，Microcode的产生和存储都有不小的损耗；而且，工程师们发现，这些复杂指令也没有那么常见，CISC中只有20%的简单指令是高频使用的。于是，在RAM技术逐渐成熟之后，RISC就应运而生了，不过这就是另一个话题了，这里不多作展开。

而随着DRAM技术的出现和发展，RAM不再是指令执行的瓶颈，提升CPU效率的重点变成了提升CPU本身执行命令、完成计算任务的能力。Intel、AMD等厂商发现在CPU内部重建DAG（这个后面我们会再去提）并按其执行能提高效率，于是搞出了指令的动态DAG和乱序执行（OoO）；为了消除伪数据依赖，搞出了寄存器重命名；为了提前预测分支走向、减少流水线Stall的时间，搞出了分支预测；利用并发思想、透过布置若干套平行的模块，把DAG中可以并行执行的指令同时发射出去执行，收集它们，然后按照顺序提交，也就是“超标量”。这些工作主要都集中在提升ILP（指令级并行）能力上，确实有用，但空间相对有限。

在这些做完之后，工程师们发现，得益于OoO的实现，通过极限优化流水线、加深流水线级数，CPU的主频还能进一步提升；这套技术我们称之为超流水线。而每次透过加深流水线深度来增加主频之后，CPU的IPC就可以以极高的性价比提升一截，进而提升性能。于是，业界开始了著名的“主频大战”，认为高主频=高性能；工程师则在老板们的催促下，在这条甜品之路上狂飙突进了若干年，把主频越堆越高，流水线深度越堆越深。2004年，基于NetBurst架构的Intel Pentium 4（奔腾4）就是这条道路的登峰造极之作，流水线深度达到了整整31级，堪称丧心病狂。

但这条路也有几个硬伤。首先，流水线越深，意味着同一时刻在流水线内的指令数量越多；而一旦某个快要执行完的指令（比如31级流水线里的第30级指令）的分支预测猜错了，那么后面所有的指令全部要Flush掉推倒重来，这种剧烈的惩罚导致IPC反倒被拉下来了，高频并不意味着高性能。然后是功耗墙：众所周知，晶体管数量越多、结构越复杂，由于电学效应，其热密度会越来越高，需要的功耗也水涨船高。而超多层级的流水线需要大量的电路来做调度工作——维护庞大的乱序执行逻辑、复杂的寄存器重命名表、动态的DAG、高频时钟树——这导致了对晶体管的数量需求和结构复杂度飙升，进而推高了功耗；而由于电源供电能力和散热能力都有限，功耗显然不能无限攀升。于是，奔腾4被用户们戏称为“大火炉”（骁龙888并感），并在商业上大暴死（被隔壁只有12级流水线、还支持x86_64的速龙64吊打），Intel被迫取消后续4GHz版本的研发计划。

在那之后，Intel痛定思痛，基于奔腾M开发出了只有14级流水线的酷睿架构，并结合更宽的译码器和宏融合等技术，实现了更高的能效比，夺回了一城。随后，随着Intel首先推出双核CPU（“胶水双核”奔腾D，以及大获成功的Q6600），CPU内部总线架构的成熟，以及Windows Vista开始对多线程进行针对性优化调度，大伙发现，并发思想在这里再度大放异彩——如果一个核心（原来的CPU）不够，那就多搞几个，然后用高效的总线把它们连起来，又能再战20年。

不过这会大伙面临一个问题：这回CPU不能反向把已经整合进去的十几级流水线、庞大的译码器、DAG重建和OoO、寄存器重命名、分支预测等功能全部砍了吧？但如果不砍的话，CPU的一个核心就太大了，die面积、热功耗和总线的带宽、延迟都无法支持我们无限往上堆核心。这对某些数据密集型任务其实相当不友好——很多运算其实没有复杂的条件逻辑和跳转逻辑，调度起来不算难，但数据量超大。

而从更早以前开始的另一个故事，让我们意识到了一些全新的可能性……

#### GPU的诞生

故事要从上世纪90年代说起。那时，计算机图形学取得突破性进展，越来越多的软件开始利用这些图形学技术美化UI，电子游戏也在这个时候逐渐开始出现并风靡起来。图形计算的本质是算像素点——屏幕里那几十万、几百万个像素，每个应该是什么颜色？整体应该呈现出怎样的画面？而每个像素点，都是根据某种运算规则，由原始数据经过某种图形学运算（很多都是经典的浮点运算和矩阵运算），互相独立地计算出来的。那么我们可以总结出这东西有什么特点：

- 数据密集：图形相关的数据量相当大，从早期的贴图再到现在的详细的3D纹理和2DCG
- 运算简单：大部分是常规的数学运算，没有很复杂的跳转和条件逻辑
- 没有依赖关系：每个像素点之间的结果不互相依赖



#### 从GPU到DSA：各种协处理器的诞生

#### SoC的诞生和异构运算的登峰造极——Apple Silicon

#### 异构：从实践到思想

### Transformer：为什么说它是“AI体系的CPU”

### 为什么我们需要系统工程和异构思想