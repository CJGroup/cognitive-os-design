# 认知OS：顶层设计

## 前言：Transformer的问题

在之前与AI的Brainstorming中，我提出了一个有趣的想法：Transformer在抽象不同的模式、从大量无规律数据（Token序列）中提取隐式规律，在不同的符号体系间迁移规律这几件事上干的非常棒，这也是为什么现在以LLM为首的Transformer系AI如此强大和火热的根本原因。

但Transformer自己无法做到以下几件它内禀就没法很好完成的事情：

- 严密的逻辑推理（因为它本质上是基于统计概率的，不能做到100%的严丝合缝；CoT虽然改善了一部分前向传播的回溯问题，但整个体系依然是难以回溯的；且自然语言由于歧义性的存在，也不是逻辑推理的最佳载体）
- 持续学习，也就是从交互和学习中内化记忆，并固化成常识和经验的能力，毕竟梯度下降和“训练-推理”的二元对立就很不适合干这个
- 对我们生存的这个世界没有物理概念和直觉（也就是“世界模型”）
- “万物皆Token，皆可Tokenize”催生了Transformer生态的繁荣，但计科的历史告诉我们，通用表示作为一种抽象，必然会导致特定结构的隐性化。显然，Transformer需要花费额外的工夫（Attention机制的算力），透过对大量标记数据的学习，在其网络内部透过Attention机制，重新把隐藏在无结构数据中的隐式结构显性化。这就带来了额外的算力消耗。
- Transformer的Attention机制，以及“Token需要两两交互”的原理决定这套系统是反稀疏的，必然导致整套系统在算力消耗上会持续膨胀。任何MoE和其他“模拟稀疏计算”的工程手段必然是治标不治本。

而对这些问题，我提出了一个有趣的想法：认知系统（Cognitive OS）。接下来，我将对这个概念进行详细阐述。

## CPU之喻：用计算机体系架构进行类比

在现有的AI应用架构中，模型需要处理很多事情：处理文本或其他模态的输入，计划、执行、决策、评价、以需要的东西输出，而且它是唯一的”操作者“，其他的组件只是它操作的“工具”，中间甚至是用文本形式进行数据交换的。而我认为，Transformer由于其“万物皆可Tokenize”的特性，意外通过scaling表现出了很强的通用性。就像计算机体系结构中的CPU一样，在计算能力强化到一定程度之后，发现它可以处理通用任务了。

但随着时间推移，我们会发现，CPU虽然很通用，但很多特定的负载它没法很好的处理，或者说，至少不是处理这类任务的最佳架构。

比如，CPU单个核心为了**以最低的延迟处理各种稀奇古怪的计算任务**，提高单线程计算能力，把流水线设计的十分复杂，有强大的分支预测功能，拥有充裕的多级缓存，同时为CPU设计了复杂的CISC指令集。但也因此，代价来了：

1. 每个CPU核心的晶体管数目下不来，die面积自然也就小不了。在有限的CPU整体die面积下，能塞进去的核心数量自然有限。
2. CPU整体到内存的数据传输带宽是有限，核心堆太多，瓶颈就会转移到内存带宽，以及CPU各核心间环形总线/网状总线的数据交换带宽上。
3. 而且，因为其流水线结构十分复杂，调度和上下文切换的开销很大。多级缓存一方面减少了对内存读取的需求，但另一方面，在上下文切换时，新线程会发现L1/L2缓存里全是旧线程留下的“数据残渣”，只能眼睁睁看着缓存命中率暴跌，被迫重新从内存慢吞吞地加载数据，这种缓存冷启动和潜在的TLB抖动，进一步放大了切换代价。

处理简单问题时，虽然单个核心处理这类问题的能力依然很强，但并发度上不来。但图形运算的本质是计算每一个像素点要呈现的内容，因此需要的就是大规模但每个任务的复杂度不算重的计算设备。

进一步打个比方，就是一个CPU核心是一个**名牌大学博士生**，个体能力强悍，能够快速解出各种稀奇古怪的数理问题，但有以下缺点：

- 因为培养成本太高，需要更高的工资待遇去养，预算有限，所以不能养太多。
- 他们解题时习惯把草稿纸摊满整张桌子
- 他们解题时习惯把草稿纸（寄存器）摊满整张桌子，换题时不仅要“缓过神”，还得把旧草稿清掉、重新铺纸——这个过程本身就要耗费不少时间。如果不让他缓神的话，可能一不小心就把上一道题的条件或者公式代入了这道题，导致结果通盘是错的；或者无法有效利用之前计算出来的中间结果，导致解题效率低下。
- 他需要查资料时，还得受制于”进图书馆要排队”这个无厘头的限制。而且，如果你需要好几个博士生互相协作的话，他们之间互相讨论还需要同步草稿纸的内容，效率可想而知。

而图形计算的工况，则更像是你需要**一秒钟交出几千张满分的口算题卡**。博士生虽然能解微积分，但面对一秒钟几千张口算卡，他的深思熟虑反而成了累赘，而“数量有限”和“每次做题都需要大张旗鼓”就成了致命缺陷。很显然，CPU的重型核心就不适合干这种活。

于是，意识到CPU核心这个“博士生”在面对海量重复且低端的劳动时的无力感后，芯片架构师们萌生了一个叛逆的想法：**既然通用计算在此处如此低效，何不打造一台专为“题海战术”而生的专用机器？**这便是GPU作为异构计算组件的起点。它的核心被刻意“阉割”掉复杂的控制逻辑与分支预测，只保留对浮点运算（FP32/FP16）的极致肌肉记忆，辅以少量整数逻辑用于寻址。但作为补偿，芯片设计师在一颗硅片上塞进了成百上千个这样的“核心”。

为了管理这支大军，GPU引入了SIMT（单指令多线程）调度策略——它像一位严厉的教官，要求所有人齐步走、做同样的动作；遇到有人卡壳时，教官会立即换下一排人顶上，确保训练场永不空置。CPU只需下达一次“开始渲染”的军令，GPU便能自行调度内部数千个核心独立吞吐数据，**极大地降低了对CPU“中断”和“上下文切换”的依赖**。至此，图形运算的“数量级”难题，终于遇上了“核海战术”的最优解。

在GPU的成功案例之后，工程师们逐渐学会了”把不同类型的任务交给专门为此设计的硬件去做“。比如，嫌CPU转发网络数据包不够快？那就搞出专门的ASIC电路来”硬件Offload“CPU的数据包转发，后来甚至有了DPU；嫌CPU和GPU处理AI时代的矩阵运算太慢？研发专门的“张量处理单元”（TPU），只为矩阵运算而生；GPU跑AI功耗太高？那就研发NPU，专门为了推理而优化，适合部署在端侧；CPU处理无线信号的信号调制解调效率太低？那就上专门的基带芯片和无线网卡。而靠着PCIe总线（以及后来SoC时代的片内总线），和OS层的各种精心设计——标准化的API，硬件抽象层（HAL），可插拔的驱动架构，事件总线，调度器——让这些组件可以高效的协同运作。

这不仅仅是一种优秀的实践，更是一种优秀的、来自计算机科学的设计思想——异构运算。这个思想的本质是，CPU这种通用运算组件都会落入各种号称”通用“的东西的必然宿命——广而不深；在CPU这个领域，就是”能干大部分任务，但在处理部分任务时效率和结果良率都不行“。而在处理特定任务时，为此专门设计的芯片往往效率能高上CPU千百倍，但代价是牺牲通用性，只能干着一种活。

然而，只要能透过某种方式进行高效的调度、任务路由和数据交流，那么，让一群擅长各自工况的、专门设计的组件协同运作，就可以靠着”把任务交给最适合它的计算单元去执行”，要比单个巨无霸通用组件执行同一个任务，要来的更经济、更高效。而靠着对不同专用组件的高效任务编排和调度，异构计算体系甚至在系统整体层面，比单个CPU的“通用”更进一步。

Apple的M系列桌面端SoC就是这个思想的集大成之作——也许CPU和GPU的绝对性能不是最强的，但透过各种专门为各种特定工况设计的协处理器，基于UMA的高效数据传输，以及硬件层MacOS为此专门优化的调度器，